# Frontend API base URL used at build/runtime by Vite.
# For same-domain Nginx proxying /api -> Django, keep /api.
VITE_API_BASE_URL=/api

# Dev-only Vite proxy target.
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
